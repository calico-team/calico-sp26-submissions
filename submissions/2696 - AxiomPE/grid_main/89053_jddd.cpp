#include <bits/stdc++.h>
using namespace std;

void slv() {
    string s;
    cin >> s;
    cout << s[2] << "\n";
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    int t;
    if (cin >> t) {
        while (t--) slv();
    }
    return 0;
}
