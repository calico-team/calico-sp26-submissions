#include <iostream>
#include <cstdio>
#include <queue>
#include <vector>
#include <utility>



int main()
{
    int t, l, w, e, r, c, m;
    std::cin >> t;
    while (t > 0)
    {
        --t;
        std::cin >> l >> w >> e >> r;
		m = e / (l * 2 + w * 2);
        c = e;!(l * 2 + w * 2);
        if (c = 0)
        {
            break;
        }
        m + 1;

    }
}
